# Required: Digitransit subscription key
DIGITRANSIT_KEY=

# Optional: HTTP port (default 4001)
PORT=4001

# Optional: board cache TTL in ms (default 60000)
#
# Short, because a departures board wants freshness and TRMNL polls
# infrequently.
CACHE_TTL_MS=60000
